apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  labels:
    app: drvision-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  labels:
    app: drvision-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: drvision-postgres
  template:
    metadata:
      labels:
        app: drvision-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - name: POSTGRES_DB
              value: "drvision"
            - name: POSTGRES_USER
              value: "drvision"
            - name: POSTGRES_PASSWORD
              value: "drvision_dev"
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: drvision-postgres
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: drvision-postgres
